# wireproto workspace

The task brief you received is the complete specification. This repo contains
only illustrative material:

- `captures/session1.stream` — a short capture of four valid frames from a
  real gateway session, so you can eyeball the framing and escaping. It is an
  illustration, not test data; build nothing that special-cases it.

Deliverable: a single `wireproto.py` in this directory, exactly as the brief
specifies.
