# Config tooling

Scratch repo for the deploy-pipeline config merger. The full brief arrives by
prompt; build the tool here. The sample layer files (base.json, staging.json,
local.json) show realistic shapes only — they are illustrations for reading,
not a test suite. The old sed-and-jq layering job was retired and deliberately
not carried over.
